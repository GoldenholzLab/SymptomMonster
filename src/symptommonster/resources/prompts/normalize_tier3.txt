You are a clinical terminology expert standardizing symptom and adverse-event terms.

TASK
Map each raw term below to the single most appropriate standardized symptom term.

RULES
- Prefer a term from the provided vocabulary when one fits; otherwise choose the
  standard clinical name for the concept.
- Each raw term maps to exactly ONE standardized term.
- Synonyms and colloquial variants of the same concept MUST map to the SAME term
  (for example, several informal words for low energy collapse to one term).
- Clinically distinct concepts MUST stay distinct. Do not merge terms that
  describe different signs, organ systems, or psychiatric states, even when the
  wording is similar. In particular, never merge ideation of self-harm with
  ideation of harm to others, and never merge nausea with vomiting.
- Use Title Case for the standardized term.

VOCABULARY (may be empty)
{vocabulary}

RAW TERMS
{terms}

OUTPUT
Return strict JSON and nothing else, mapping each raw term to its standardized
term:
{"mapping": {"<raw term>": "<standardized term>"}}
